<%- include('../layouts/header') %>
    <!-- Page Heading -->
    <h1 class="h3 mb-4 text-gray-800">Add Questions To Contemplate</h1>

    <div class="col-lg-6" style="padding-right: 0;
padding-left: 0;">

        <!-- Circle Buttons -->
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary"></h6>
                <% if(success !='' ){ %>
                    <div class="alert alert-success" style="margin-bottom: 0;" id="error">
                        <%= success %>
                    </div>
                    <% } %>
                        <% if(error !='' ){ %>
                            <div class="alert alert-danger" style="margin-bottom: 0;" id="error">
                                <%= error %>
                            </div>
                            <% } %>
            </div>
            <div class="card-body">
                <form id="QuestionsToC" method="post" enctype='multipart/form-data'>
                    <div class="form-row">
                        <div class="form-group col-md-5">
                            <label>Questions To Contemplate Type : </label>
                            <select id="type" name="type" class="form-control">
                                <option value="" selected>Select Type</option>
                                <option value="Clarity">Clarity</option>
                                <option value="Courage">Courage</option>
                                <option value="Activation">Activation</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Front Questions To Contemplate Image : </label>
                        <input type="file" style="line-height: 1.15;" class="form-control" id="frontImage"
                            name="frontImage" accept="image/jpg, image/jpeg, image/png">
                    </div>
                    <div class="form-group">
                        <label>Flip Questions To Contemplate Image : </label>
                        <input type="file" style="line-height: 1.15;" class="form-control" id="flipImage"
                            name="flipImage" accept="image/jpg, image/jpeg, image/png">
                    </div>
                    <button type="submit" class="btn btn-primary mt-3">Submit</button>
                </form>
            </div>
        </div>


    </div>

    <script src="/admin/vendor/jquery/jquery.min.js"></script>

    <script>
        $('#pageTitle').text('Add Questions To Contemplate')
        $('#select-QTC').addClass('active', 'true');


        $(document).ready(function () {

            jQuery.validator.addMethod("noSpace", function (value, element) {
                return value == '' || value.trim().length != 0;
            }, "No space please and don't leave it empty");

            $.validator.addMethod("imageExtension", function (value, element) {
                var allowedExtensions = ["jpg", "jpeg", "png", "gif"];
                var fileExtension = value.split('.').pop().toLowerCase();
                return $.inArray(fileExtension, allowedExtensions) !== -1;
            }, "Please select a valid image file (JPG, JPEG, PNG)");


            $("#QuestionsToC").validate({
                rules: {
                    type: {
                        required: true,
                    },
                    frontImage: {
                        required: true,
                        imageExtension: true
                    },
                    flipImage: {
                        required: true,
                        imageExtension: true
                    },
                },
                messages: {
                    type: {
                        required: "Please select type",
                    },
                    frontImage: {
                        required: "Front questions to contemplate image is required",
                    },
                    flipImage: {
                        required: "Flip questions to contemplate image is required",
                    },
                },
                submitHandler: function (form) {
                    form.submit();
                }
            })
        })
    </script>


    <%- include('../layouts/footer') %>